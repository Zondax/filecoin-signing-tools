[workspace]
members = [
    "signer",
    "signer-ffi",
    "signer-npm",
    "hfuzz-signer",
    "extras",
]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
overflow-checks = true
